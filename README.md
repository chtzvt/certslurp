# ctsnarf

`ctsnarf` is a tool for distributed scraping/retrieval of Certificate Transparency logs. 

Ctsnarf is composed of:

`ctsnarfd`: The daemon process responsible for performing scraping scraping work.

`ctsnarfctl`: CLI for managing a cluster of `ctsnarf` processes.