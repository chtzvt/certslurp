# certslurp

`certslurp` is a tool for distributed scraping/retrieval of Certificate Transparency logs. 

certslurp is composed of:

`certslurpd`: The daemon process responsible for performing scraping scraping work.

`certslurpctl`: CLI for managing a cluster of `certslurp` processes.